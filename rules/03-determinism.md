# Rule 3 - Deterministic before generative

Inventories, dependency graphs, CRUD matrices, copybook offsets and record decodes come from
parsers. They are re-runnable and byte-identical, which is what makes an audit two years
later possible.

Use the model for judgement, not for arithmetic. If you find yourself counting, measuring or
computing an offset in prose, stop and run the script.
